# Pi to Server Pubnub Message

```json
{
    "datetime": "datatime ISO 8601 format",
    "compartment": number,
    "sensor": "sensor name",
    "isopen": bool "if sensor is a switch",
    "isempty": bool "if sensor is FSR"
}
```